ggplot_build:
    panel$ranges[[1]]$x.range <- c(0, 1)
    panel$ranges[[1]]$x.labels <- c("0.00", "0.25", "0.50", "0.75", "1.00")
    
train_ranges
coord_train
continuous_scale

too late:
flip_labels    